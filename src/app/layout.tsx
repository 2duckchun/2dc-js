import React from 'react';
import 'normalize.css';
import '@/styles/globals.scss';
import '@/styles/animations.scss';

import Footer from '@/components/rootLayout/Footer';
import Header from '@/components/rootLayout/Header';
import { font_M_PLUS_1, font_pretended } from '@/fonts/fonts';
import useInitTheme from '@/hooks/server/useInitTheme';

import style from './layout.module.scss';

export const metadata = {
  title: '2DCJS',
  description: 'Generated by create next app',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  const theme = useInitTheme();

  return (
    <html lang='ko' data-theme={theme.value}>
      <body className={`${font_M_PLUS_1.className}`}>
        <Header currentTheme={theme.value} />
        <main className={`${font_pretended.className} ${style.page}`}>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
